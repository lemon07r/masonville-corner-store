<style>
:root {
  color-scheme: light;
  --color-bg: #fff9f1;
  --color-surface: #ffffff;
  --color-text: #1f1f1f;
  --color-accent: #e89b1a;
  --color-accent-dark: #b8770c;
  --color-forest: #2f6f59;
  --font-base: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}

@media (prefers-reduced-motion: no-preference) {
  :root {
    scroll-behavior: smooth;
  }
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: var(--font-base);
  background: var(--color-bg);
  color: var(--color-text);
  min-height: 100vh;
}

a {
  color: inherit;
  text-decoration: none;
}

header,
main {
  max-width: min(1200px, 90vw);
  margin: 0 auto;
}

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  padding: 0.85rem 1.5rem;
  border-radius: 999px;
  font-weight: 600;
  background: var(--color-accent-dark);
  color: #fff;
  transition: transform 150ms ease, background 150ms ease;
  box-shadow: 0 2px 8px rgba(184, 119, 12, 0.25);
}

.btn:hover,
.btn:focus-visible {
  background: #a06708;
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(184, 119, 12, 0.35);
}

.hero {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 2.5rem;
  align-items: center;
  padding: 6rem 0 4rem;
}

.hero__headline {
  font-size: clamp(2.5rem, 4vw, 3.5rem);
  margin-bottom: 1rem;
  line-height: 1.05;
}

.pill {
  display: inline-flex;
  align-items: center;
  gap: 0.35rem;
  padding: 0.35rem 0.75rem;
  border-radius: 999px;
  background: rgba(47, 111, 89, 0.12);
  color: var(--color-forest);
  font-weight: 600;
  font-size: 0.85rem;
  letter-spacing: 0.015em;
}

.section {
  padding: 4rem 0;
}

.reviews {
  display: grid;
  gap: 1.5rem;
}

.review-card {
  background: var(--color-surface);
  border-radius: 1.25rem;
  padding: 1.75rem;
  box-shadow: 0 22px 60px -40px rgba(31, 31, 31, 0.45);
}

.grid {
  display: grid;
  gap: 1.5rem;
}

@media (min-width: 768px) {
  .grid--cols-3 {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }
}
</style>
